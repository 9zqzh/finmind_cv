# 学院教学小助手 · 技术栈说明

> 适用读者：第一次接触本项目的开发者
> 编写依据：项目内真实代码与配置文件（版本号为当前开发环境实测值）
> 关联文档：《项目结构说明.md》《业务功能说明.md》《后端后续开发操作指南.md》

---

## 一、总览：一张图看懂系统组成

```text
浏览器（用户）
   │  Docker: http://localhost:8080；本地开发: http://localhost:5173
   ▼
前端（React + Vite + Ant Design）          ← frontend
   │  /api/* 请求经 Vite 开发代理转发
   ▼
后端（FastAPI + PydanticAI Agent）          ← backend
   ├── DeepSeek 大模型（OpenAI 兼容接口）   ← Agent 的"大脑"
   ├── gduf-jwxt-api（教务系统客户端）     ← PyPI 依赖
   ├── gduf-web-api（学院官网客户端）      ← PyPI 依赖
   ├── gduf-academic-api（学术资源客户端） ← packages/gduf-academic-api
   └── 本地知识库文件（.md/.txt/.json）     ← data/knowledge、data/information
```

三个关键名词的通俗解释：

- **Agent（智能体）**：能"听懂"自然语言、自己决定调用哪个工具、再把结果组织成人类可读回答的程序。本项目的大模型是 DeepSeek，编排框架是 PydanticAI。
- **Tool Calling（工具调用）**：大模型本身不知道你的课表，它只能"决定"去调用哪个函数（如 `query_schedule`），真实数据由函数返回。
- **RAG（检索增强生成）**：先从本地资料里检索相关片段，再让大模型基于检索结果作答，避免模型"编造"。

---

## 二、后端技术栈

### 2.1 语言与运行环境

| 项目 | 版本 | 说明 |
|---|---|---|
| Python | ≥ 3.10（本机实测 3.12.10） | `pyproject.toml` 中 `requires-python = ">=3.10"`，代码使用了 `X | None` 新式类型标注 |
| 虚拟环境 | `.venv`（位于 `backend/.venv`） | 用 `python -m venv` 创建，依赖与系统隔离 |

### 2.2 核心框架与库（均为当前环境实测版本）

| 技术 | 版本 | 用途 | 选型理由 |
|---|---|---|---|
| **FastAPI** | 0.141.1（声明 `>=0.111`） | Web 框架，提供 HTTP 路由、参数校验、自动 OpenAPI 文档 | Python 生态与 AI SDK 契合；`/docs` 自带交互调试页，联调成本低 |
| **Uvicorn** | 0.52.1（声明 `>=0.30`） | ASGI 服务器，运行 FastAPI 应用 | FastAPI 官方推荐组合，开发期支持 `--reload` 热重载 |
| **PydanticAI** | 2.24.0（声明 `>=1.0,<3`） | Agent 编排框架：注册工具、驱动模型多轮工具调用 | 与 Pydantic 同门，工具参数即 Python 类型标注，开发体验好 |
| **Pydantic** | 2.13.4 | 请求/响应模型定义与数据校验 | FastAPI 与 PydanticAI 的共同基础 |
| **pydantic-settings** | 2.14.2（声明 `>=2.0`） | 配置管理：从 `.env` 文件和环境变量读取配置 | 密钥不进代码、不进 Git（见第四节） |
| **openai**（官方 SDK） | 2.53.0 | 以 OpenAI 兼容方式调用 DeepSeek | PydanticAI 的 `OpenAIChatModel` + `OpenAIProvider` 基于它构建 |
| **httpx / beautifulsoup4** | 0.28.1 / 4.15.0 | `gduf-jwxt-api` 与 `gduf-web-api` 的运行依赖（HTTP 请求与 HTML 解析） | 随 PyPI 包自动安装 |

### 2.3 PydanticAI 的使用方式（2.x 版本）

- **Agent 构建**（`app/agent/orchestrator.py`）：`Agent(model, deps_type=AgentDeps, system_prompt=...)`，全局单例懒加载。
- **工具注册**（`app/agent/tools.py`）：用 `@agent.tool` 装饰器注册 14 个工具；工具函数第一个参数是 `RunContext[AgentDeps]`，可访问会话、知识库等依赖。
- **依赖注入**：每次对话构造一个 `AgentDeps`（含当前用户会话、知识库、资讯库实例），通过 `agent.run(message, deps=deps)` 传入。
- **错误处理约定**：工具内部业务失败**不抛异常**，而是返回 `{"error": "..."}` 字典，让模型自己组织可操作的提示（如"请先登录"）。
- **两个版本坑**（已写入操作指南，新人务必注意）：
  1. PydanticAI 2.x 的异常模块**没有** `ToolError`，不要导入它；
  2. 不要用 `asyncio.wait_for` 包裹 `agent.run()`，会与框架内部任务图冲突导致假性超时，超时控制依赖 OpenAI 客户端自身的 timeout 参数。

### 2.4 DeepSeek 模型接入

- **接入方式**：OpenAI 兼容接口（不是 DeepSeek 专属 SDK），实现见 `app/agent/model_client.py`：

```python
client = AsyncOpenAI(base_url=settings.deepseek_base_url,
                     api_key=settings.deepseek_api_key,
                     timeout=settings.agent_timeout_seconds)
model = OpenAIChatModel(settings.deepseek_model,
                        provider=OpenAIProvider(openai_client=client))
```

- **模型名**：默认 `deepseek-v4-flash`（实测可用；`deepseek-chat` 同样可用）。换模型只改 `.env` 中的 `DEEPSEEK_MODEL`，不动代码。
- **好处**：将来换成任何 OpenAI 兼容服务，只需改 `DEEPSEEK_BASE_URL` 和模型名。

### 2.5 gduf-jwxt-api（教务系统客户端）

- **是什么**：PyPI 包 [`gduf-jwxt-api==0.2.0`](https://pypi.org/project/gduf-jwxt-api/)（导入名 `jwxtapi`），封装广东金融学院综合教务管理系统的登录与查询：验证码获取、学号密码登录、会话保持、个人课表、教室课表、成绩、成绩明细、培养方案、教学楼列表。
- **关键特点**：**同步** httpx 客户端，返回值是冻结 dataclass（可 `to_dict()`）；一套明确的 `JwxtError` 异常体系。
- **在本项目中的位置**：后端通过 `app/adapters/jwxt.py` 适配层调用它——用 `asyncio.to_thread` 把同步调用包装成异步，把它的异常统一映射为后端错误码。
- **安装方式**：已精确声明在 `backend/pyproject.toml`，安装后端时自动从 PyPI 安装。源码和 API 文档见[上游仓库](https://github.com/LiangJZ1/gduf-jwxt-api)。

### 2.6 gduf-web-api（学院官网与竞赛平台客户端）

- **是什么**：PyPI 包 [`gduf-web-api==0.2.1`](https://pypi.org/project/gduf-web-api/)（导入名 `gduf_web_api`），封装学院官网（https://ai.gduf.edu.cn/）的公开内容抓取与搜索（AI 搜索、文章详情、师资列表、新闻列表等），以及学院竞赛管理与问答平台（`aijspt`，ai-data-competitions.cn）的公开信息检索（比赛列表/详情、通知公告、竞赛社团）。
- **关键特点**：**同步** httpx 客户端，无需登录；返回值是冻结 dataclass（`ArticleSummary`/`PageResult`/`ListResult`/`CompetitionSummary`/`CompetitionDetail`/`Notice`/`ClubSummary` 等）；异常体系 `GdufError` > `NetworkError`/`ParseError`/`InvalidPageError`/`UnsupportedSourceError`；内部按来源适配器协议（`SourceAdapter`/`CompetitionSourceAdapter`）组织，新增来源不破坏既有 `get_ai_*`/`get_aijspt_*` 行为。
- **在本项目中的位置**：后端通过 `app/adapters/gduf_web.py` 适配层调用它——同样用 `asyncio.to_thread` 包装同步调用，异常映射为 `ApiError`。官网搜索（`search_website`）与竞赛平台四类查询（`query_competitions` 比赛列表、`query_competition_detail` 单场详情、`query_competition_notices` 通知公告、`query_competition_clubs` 竞赛社团）均已注册为 Agent 工具。模块级 `GdufClient` 懒加载复用。另：竞赛平台对无效 ID/URL 抛 `ValueError`，适配层在 `run_gduf` 中统一归一为 `INVALID_PARAM`。
- **安装方式**：已精确声明在 `backend/pyproject.toml`，安装后端时自动从 PyPI 安装。源码和 API 文档见[上游仓库](https://github.com/LiangJZ1/gduf-web-api)。

### 2.7 gduf-academic-api（学术资源客户端）

- **是什么**：项目自带的本地 Python 包（`packages/gduf-academic-api`），聚合搜索 arXiv、Semantic Scholar 等开放学术平台的论文与文献资料。
- **关键特点**：**同步** httpx 客户端，无需登录；支持 `proxy` 参数配置代理；采用可扩展注册表架构，新增平台只需在 `platforms/` 目录下创建模块文件并用 `@register` 装饰；异常体系 `AcademicError` > `NetworkError`/`ParseError`/`ValidationError`/`UnsupportedPlatformError`。
- **在本项目中的位置**：后端通过 `app/adapters/academic.py` 适配层调用它——同样用 `asyncio.to_thread` 包装同步调用，异常映射为 `ApiError`。模块级 `AcademicClient` 懒加载复用。
- **安装方式**：在仓库根目录可编辑安装 `backend/.venv/Scripts/python -m pip install -e packages/gduf-academic-api`。

### 2.8 测试框架

| 工具 | 版本 | 说明 |
|---|---|---|
| pytest | 9.1.1（声明 `>=8.0`） | 后端测试入口，`pytest.ini_options` 配置在 `pyproject.toml` |
| pytest-asyncio | 1.4.0（声明 `>=0.23`） | 支持异步测试，`asyncio_mode = "auto"` 免去逐个标注 |

测试位于 `backend/tests/`，当前共 72 个用例，覆盖知识检索、HTTP 接口、Agent、外部适配层、空闲教室、纯文本降级、对话记忆与 PostgreSQL 持久化。运行：`.venv\Scripts\python -m pytest`。

---

## 三、前端技术栈

前端工程位于 `frontend`，实测版本如下：

| 技术 | 版本 | 用途 | 选型理由 |
|---|---|---|---|
| **React** | 18.3.1 | UI 框架 | 组件生态成熟，适合快速原型 |
| **TypeScript** | 5.9.3 | 开发语言，`tsconfig.json` 开启 strict | 前后端共用接口类型定义（`src/api/types.ts`），减少联调错误 |
| **Vite** | 5.4.21 | 构建工具与开发服务器 | 启动快；开发期把 `/api` 代理到后端 8000 端口，天然解决跨域 |
| **Ant Design (antd)** | 5.29.3 | UI 组件库 | Table/Form/Card/Tabs/Message 等工具型组件齐全，中文 locale 已配置 |
| **@ant-design/icons** | 5.6.1 | 图标库 | 随 antd 使用 |
| **axios** | 1.19.0 | HTTP 请求 | 拦截器统一附带 `X-Session-Token` 请求头、统一拆包 `{success, data, message}` 响应 |
| **react-router-dom** | 6.30.4 | 前端路由 | 登录页与主应用分离，未登录自动跳转 `/login` |

前端命令（在 `frontend` 目录）：

| 命令 | 作用 |
|---|---|
| `npm install` | 安装依赖（依赖锁定在 `package-lock.json`，可用 `npm ci` 复现） |
| `npm run dev` | 启动开发服务器（http://localhost:5173，带热重载） |
| `npm run build` | 类型检查（`tsc --noEmit`）+ 生产构建 |
| `npm run preview` | 预览生产构建产物 |

**注意**：Docker 运行时统一访问 `http://localhost:8080`；只有本地 Vite 开发模式使用 `http://localhost:5173`。

---

## 四、数据与存储方案

原型阶段刻意不引入数据库，全部采用"文件 + 内存"的轻量方案：

### 4.1 知识库与资讯（文件存储 + 向量检索/关键词兜底）

- **数据格式**：`.md` / `.txt` / `.json`，分别放在 `backend/data/knowledge/`（制度流程、学院简介等）和 `data/information/`（学院资讯、竞赛信息）。
  > ⚠️ 当前两个目录里只有**示例数据，待替换**为真实学院资料。
- **检索方案**（`app/knowledge/service.py`）：启动时加载全部文件 → Markdown 按标题切片、超长段按 400 字符二次切分、JSON 按记录切片。配置完整嵌入服务时由 Chroma 执行向量相似度检索；否则用中文双字（bigram）+ 英数单词关键词评分返回 top-k。
- **运行策略**：Chroma、嵌入服务或向量查询异常时自动回退关键词检索。Windows 本地开发建议使用 Docker Chroma，具体步骤见《团队本地运行与向量检索指南》。

### 4.2 会话管理（PostgreSQL + 加密 Cookie）

- **实现**：`SessionManager` 将令牌摘要和 Fernet 加密后的教务 Cookie 保存到 PostgreSQL，进程内仅缓存已恢复的 `JwxtClient`；`ConversationManager` 持久化用户会话与完整消息。
- **TTL 机制**：验证码阶段使用 `SESSION_TTL_MINUTES`，登录会话按 `LOGIN_SESSION_TTL_DAYS`（默认 7 天）滚动续期，同时受教务系统 Cookie 有效期限制。
- **迁移**：数据库结构由 Alembic revision 管理，启动入口先升级到 head，再启动 API。

### 4.3 前端本地存储

仅用 `sessionStorage` 保存 `session_token`（关闭标签页即失效）；密码、验证码不落盘。

### 4.4 与规划文档的差异说明（以代码为准）

《技术选型与五人分工规划》4.2 节提到"SQLite + SQLAlchemy/SQLModel"，但**当前骨架未实现持久化存储**（无 `storage/` 目录），会话与成绩缓存均在内存中；这属于原型阶段有意的简化。

---

## 五、开发工具链

### 5.1 后端安装步骤（从零开始）

```powershell
python -m venv backend\.venv
backend\.venv\Scripts\python -m pip install -e ".\backend[test]"
backend\.venv\Scripts\python -m pip install -e .\packages\gduf-academic-api
Copy-Item .env.example .env
```

### 5.2 启动命令

```powershell
# 后端（backend 目录）
.venv\Scripts\python -m uvicorn app.main:app --reload --port 8000

# 前端（frontend 目录）
npm run dev
```

### 5.3 版本管理

- Git 仓库位于项目根目录，远端 `origin = https://github.com/9zqzh/gduf-college-assistant.git`，主分支 `main`。
- 根目录 `.gitignore` 覆盖：`.env`（保留 `.env.example`）、`.venv/`、`node_modules/`、`__pycache__/`、构建产物、日志、IDE 文件等。
- **真实 API Key 永远只写在本地 `.env`，不进 Git。**

---

## 六、关键配置项说明

配置集中在 `backend/app/config.py` 的 `Settings` 类（基于 pydantic-settings），统一读取仓库根目录 `.env`，环境变量可覆盖。

| 环境变量 | 默认值 | 生效位置 | 说明 |
|---|---|---|---|
| `DEEPSEEK_API_KEY` | （空，必填） | `config.py` → `model_client.py` | DeepSeek 平台密钥；未配置时健康检查 `model_configured=false`，构建模型会报错 |
| `DEEPSEEK_BASE_URL` | `https://api.deepseek.com` | 同上 | OpenAI 兼容接口地址 |
| `DEEPSEEK_MODEL` | `deepseek-v4-flash` | 同上 | 模型名，可按账号可用模型调整 |
| `AGENT_MAX_ITERATIONS` | `4` | `config.py`（预留） | Agent 最大迭代次数的预留配置 |
| `AGENT_TIMEOUT_SECONDS` | `30` | `model_client.py` | 作为 AsyncOpenAI 客户端的请求超时秒数 |
| `JWXT_BASE_URL` | `https://jwxt.gduf.edu.cn` | `services/session.py` 创建客户端时使用 | 教务系统地址 |
| `ACADEMIC_PROXY` | 空 | `adapters/academic.py` 创建客户端时使用 | 学术资源平台代理地址，留空直连 |
| `APP_ENV` | `development` | `main.py` 健康检查回显 | 环境标识 |
| `SESSION_TTL_MINUTES` | `120` | `services/session.py` | 会话空闲过期时长（分钟） |
| `KNOWLEDGE_DIR` | `data/knowledge` | `main.py` lifespan | 知识库资料目录（相对 backend 目录） |
| `INFORMATION_DIR` | `data/information` | 同上 | 资讯资料目录 |

其他关键配置文件一览：

| 文件 | 作用 |
|---|---|
| `backend/pyproject.toml` | 后端依赖声明（含精确固定的 `gduf-jwxt-api`、`gduf-web-api`）、pytest 配置 |
| `packages/gduf-academic-api/pyproject.toml` | 学术资源客户端依赖声明（httpx） |
| `.env.example` | Docker 与本地开发的统一配置模板 |
| `frontend/package.json` | 前端依赖与脚本（dev/build/preview） |
| `frontend/vite.config.ts` | 开发服务器端口 5173；`/api` 代理到 `http://127.0.0.1:8000` |
| `frontend/tsconfig.json` | TypeScript strict 模式、`jsx: react-jsx` |

---

## 七、技术选型回顾：为什么是这套组合

1. **FastAPI + PydanticAI + Pydantic 全家桶**：类型一致贯穿请求校验、Agent 工具参数、响应模型，联调摩擦最小。
2. **OpenAI 兼容方式接 DeepSeek**：不绑定专有 SDK，模型/地址全可配置，随时可换服务商。
3. **Chroma 向量检索 + 关键词兜底**：配置嵌入服务时提高语义召回；本地未配置服务时仍可零额外依赖运行。
4. **内存会话而非数据库**：教务客户端实例必须与登录 Cookie 绑定，内存字典最简单直接；生产化再迁移。
5. **Vite 代理 + 统一 JSON 契约**：前端不碰跨域，后端只返回 `{success, data, message, code?}` 结构，双方按冻结的接口类型并行开发。

---

## 八、知识库向量检索（2026-08-09）

- 知识库继续读取 `backend/data/knowledge/` 中已有的 Markdown/JSON；加载时按标题、段落和 400 字符上限切片，不修改 `resources/` 中的原始文件。
- `app/knowledge/embeddings.py` 定义嵌入接口，并提供 OpenAI 兼容的 `/embeddings` HTTP 适配器；真实嵌入服务由使用者自行提供。
- `app/knowledge/chroma_store.py` 只负责 Chroma collection、本地持久化、切片元数据和相似度查询。索引目录默认为 `backend/data/chroma/`，已被 Git 忽略。
- `KnowledgeService` 仍是 API 和 Agent 的唯一入口：配置完整嵌入服务时使用 Chroma；未配置或向量查询出错时自动使用原关键词检索。
- 当前本机已接入阿里云百炼 `qwen3.7-text-embedding`，由 Docker Chroma 提供本地持久化向量库；未配置嵌入服务的环境仍会自动使用关键词兜底，前端知识库页面和原始文件预览功能保持不变。

新增配置项：

| 环境变量 | 说明 |
|---|---|
| `KNOWLEDGE_RETRIEVAL_MODE` | `auto`（默认，配置嵌入服务后启用向量）或 `keyword`（始终关键词） |
| `CHROMA_DIR` | Chroma 本地索引目录，默认 `data/chroma` |
| `KNOWLEDGE_VECTOR_MIN_SCORE` | 向量检索最低相似度，默认 `0.75`；低于该值按无结果处理 |
| `EMBEDDING_BASE_URL` | OpenAI 兼容嵌入接口基础地址 |
| `EMBEDDING_API_KEY` | 嵌入服务密钥，只写本地 `.env` |
| `EMBEDDING_API_KEY_FILE` | 可选的本地密钥文件路径；未填写直接 Key 时使用，文件不会提交 Git |
| `EMBEDDING_MODEL` | 嵌入模型名称 |
| `EMBEDDING_TIMEOUT_SECONDS` | 嵌入请求超时秒数，默认 `30` |
| `EMBEDDING_BATCH_SIZE` | 单次嵌入请求的切片数，百炼 `qwen3.7-text-embedding` 默认使用 `10` |

> Windows 下建议使用本文档的 Docker Chroma 方案，避免嵌入式 Chroma 原生库兼容问题。当前已验证 Docker Chroma + 百炼嵌入的完整写入与查询链路。
